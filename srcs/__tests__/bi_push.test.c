#include "bigint.test.h"

/*
void		test_bi_push_case1(void)
{
	t_bigint	bi;
	size_t		size = 1;

	bi = bi_new(size);
	test(
		bi.size == size,
		"bi_new : bi.size"
	);

	for (size_t i=0; i < size; i++)
		test(
			bi.data[i] == 0x00,
			"bi_new : bi.data[i]"
		);
	free(bi.data);
}
*/
